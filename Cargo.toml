[package]
name = "dholman-site"
version = "0.1.0"
edition = "2024"

[dependencies]
leptos = { version = "0.8", features = ["csr"] }
leptos_router = "0.8"
pulldown-cmark = "0.13.0"

[profile.release]
lto = true
opt-level = "z"
codegen-units = 1

